

services:
  bot:
    build: ./bot
    network_mode: "host"
    secrets:
      - TOKEN
    environment:
      # путь к секрету в контейнере
      BOT_TOKEN_FILE: /run/secrets/TOKEN
    command: python bot.py

secrets:
  TOKEN:
    file: .env